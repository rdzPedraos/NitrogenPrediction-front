/* eslint-disable @typescript-eslint/no-unused-vars */
type Image = {
    id: string;
    src: null | string;
};

type FilterImage = {
    key: string;
    label: string;
    histogram?: string;
    preview: string;
};
